import { _ModuleSupport } from 'ag-charts-community';
export declare const HEATMAP_SERIES_THEME: {
    series: {
        label: {
            enabled: boolean;
            color: string;
            fontSize: _ModuleSupport.ThemeConstants.FONT_SIZE;
            fontFamily: string;
            wrapping: "on-space";
            overflowStrategy: "ellipsis";
        };
        itemPadding: number;
    };
    gradientLegend: {
        enabled: boolean;
    };
};
