import type { _ModuleSupport } from 'ag-charts-community';
import type { DefinedZoomState, ZoomProperties } from './zoomTypes';
export declare class ZoomContextMenu {
    private readonly contextMenuRegistry;
    private readonly zoomManager;
    private readonly getModuleProperties;
    private readonly getRect;
    private readonly updateZoom;
    constructor(contextMenuRegistry: _ModuleSupport.ContextMenuRegistry, zoomManager: _ModuleSupport.ZoomManager, getModuleProperties: () => ZoomProperties, getRect: () => _ModuleSupport.BBox | undefined, updateZoom: (zoom: DefinedZoomState) => void);
    registerActions(enabled: boolean | undefined, zoom: DefinedZoomState): (() => void) | undefined;
    toggleActions(zoom: DefinedZoomState): void;
    private computeOrigin;
    private onZoomToHere;
    private onPanToHere;
}
